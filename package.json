{
  "name": "docebella-livrocaixa",
  "version": "1.0.0",
  "description": "Tailwind CSS build for Django project",
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i ./static/css/input.css -o ./static/css/style.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^4.1.16"
  }
}
